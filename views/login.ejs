<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register / Log In</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,wght@0,400;0,500;0,700;1,400;1,500;1,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="styles.css"/>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.2/css/all.min.css" integrity="sha512-1sCRPdkRXhBV2PBLUdRb4tMg1w2YPf37qatUFeS7zlBy7jJI8Lf4VHwWfZZfpXtYSLy85pkm9GaYVYMfw5BC1A==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        .login-card {
            background-color: #00ff33;
            margin-top: 130px;
            margin-left: 50px;
            margin-right: 50px;
            padding-bottom: 60px;
            border-radius: 20px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        h1 {
            font-size: 50px;
        }

        input[type=text], [type=password] {
            height: 30px;
            width: 100%;
            padding: 12px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-family: inherit;
            border-radius: 10px;
            font-size: 20px;
        }

        label {
            font-size: 25px;
            padding: 12px 12px 12px 12px;
            display: inline-block;
        }

        input[type=submit] {
            font-family: inherit;
            /* height: 40px; */
            font-size: 20px;
            margin-top: 50px;
            background-color: #202731;
            color: white;
            padding: 20px;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            float: center;
        }

        .registerbutton {
            font-family: inherit;
            font-size: 20px;
            margin-top: 30px;
            background-color: #202731;
            color: white;
            padding: 20px;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            float: center;
        }

        .buttonholder {
            text-align: center;
        }

        .incorrect p {
            color: red;
            font-size: 20px;
        }

    </style>
</head>
<body>

    <%- include('nav.ejs') %>

    <div class="login-card">
        <h1>Login</h1>
        <% if (incorrect) { %>
            <div class="incorrect">
                <p>Incorrect username or password! Register or Try Again.</p>
            </div>
        <% } %>
        <form action="/authorize" method="post">
            <label for="u_name">Username</label>
            <input type="text" id="u_name" name="username" placeholder="" required>
            <label for="p_word">Password</label>
            <input type="password" id="p_word" name="password" placeholder="" required>
            <div class="buttonholder">
                <input type="submit" value="Log In">
            </div>
        </form>
        <button onclick="location.href='/register';" class="registerbutton">Register</button>
    </div>

    <%- include('footbar.ejs') %>
</body>
</html>